% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict a single new observation using a trained model}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A trained model object (e.g., from randomForest) that supports \code{predict(..., type="prob")}.}

\item{gc_content}{Numeric. The GC content of the sequence.}

\item{RNA_type}{Character. One of c("mRNA","lincRNA","lncRNA","pseudogene").}

\item{RNA_region}{Character. One of c("CDS","intron","3'UTR","5'UTR").}

\item{exon_length}{Numeric. The exon length of the RNA.}

\item{distance_to_junction}{Numeric. The distance of the site to the nearest splice junction.}

\item{evolutionary_conservation}{Numeric. A measure of evolutionary conservation of the site.}

\item{DNA_5mer}{Character. A 5‑mer DNA string (e.g., "ATCGG").}

\item{positive_threshold}{Numeric. (default = 0.5) A threshold above which the predicted probability is labelled “Positive”.}
}
\value{
A named vector of length 2:
\itemize{
\item \code{predicted_m6A_prob} — numeric probability of the “Positive” class.
\item \code{predicted_m6A_status} — character, “Positive” or “Negative”.
}
}
\description{
This function wraps around \code{prediction_multiple()} to predict for a single new observation (rather than a full data frame). It builds a one‑row data.frame from the input feature values and passes it to \code{prediction_multiple()}, then extracts and returns a named vector with two elements: \code{predicted_m6A_prob} (numeric) and \code{predicted_m6A_status} (character).
}
\examples{

ml_model <- readRDS(system.file("extdata", "rf_fit.rds", package="m6APrediction"))
  single_pred <- prediction_single(
    ml_fit = ml_model,
    gc_content = 0.50,
    RNA_type = "mRNA",
    RNA_region = "3'UTR",
    exon_length = 120,
    distance_to_junction = 8,
    evolutionary_conservation = 0.7,
    DNA_5mer = "GATCG",
    positive_threshold = 0.6
  )
  single_pred
}
